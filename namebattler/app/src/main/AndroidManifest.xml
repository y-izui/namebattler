<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.namebatter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BattleTactics"></activity>
        <activity android:name=".BattleResult" />
        <activity android:name=".BattleMain" />
        <activity android:name=".BattleStart" />
        <activity android:name=".PartyList" />
        <activity android:name=".CharacterCreateCompleted" />
        <activity android:name=".CharacterCreate" />
        <activity android:name=".CharacterStatus" />
        <activity android:name=".CharacterList" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>